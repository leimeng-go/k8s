apiVersion: v1  
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: kube-ops
data: 
  prometheus-yml: | 
    global:
      scrape_interval: 15s
      scrape_timeout: 15s
    scrape_config:
    - job_name: "prometheus"      
      static_config:
      - targets: ["localhost:9090"]  